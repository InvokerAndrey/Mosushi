// This Next.js API route is no longer used.
// Orders are submitted directly to the Django backend via the
// rewrite configured in next.config.ts: /order → http://127.0.0.1:8000/order/
// Django handles all order validation, persistence, and Telegram notifications.
export {};
