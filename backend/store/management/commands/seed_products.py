# This command has been removed.
# Products are now managed entirely through Django Admin (/admin/).
# Use the "create_default_categories" command to create the default categories,
# then add products manually via the Admin Panel.
